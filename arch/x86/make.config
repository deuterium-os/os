ARCH_CFILES := $(shell find -L * -type f -name '*.c')
ARCH_ASFILES := $(shell find -L * -type f -name '*.S')
ARCH_NASMFILES := $(shell find -L * -type f -name '*.asm')

ARCH_OBJS := $(CFILES:.c=.c.o) $(ASFILES:.S=.S.o) $(NASMFILES:.asm=.asm.o)